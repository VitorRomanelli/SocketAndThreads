# SocketAndThreads
A simple implementation of sockets using the threads concept in C.

Pre-requisites on windows: 
C compiler (gcc)

To run code paste the below line on your command line in the project folder

gcc socket.c -o socket -lpthread
